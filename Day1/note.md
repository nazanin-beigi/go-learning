1.  command for building a file (like main.go ) to Hello 
build -o (اسم فایل اجرایی قراره چی باشه ) Hello main.go --> there is no output
2.  how to run the built file?
./Hello --> see the result 
3. instead we can build and run at the same time by using ---> go run main.go
4. اگر در فانکشن ی متغییر تغریف کنم و استفاده نکنم ,  کامپایلر خطا میده 
5. declare تعریف کردن 
6. deffrence between variable and statement ?
دومی ی خط دستور اجراییه میتونه تعریف و چاپ و .. باشه مثلا x:=1
ولی در اولی x متغییره 

